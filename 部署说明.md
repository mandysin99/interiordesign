# 白模项目公开发布说明

## 两个版本入口

- 全量版：`index.html`
  - 功能：完整核对、导出、快照、测量、全部专用核对项
- 邻居版：`neighbor.html`
  - 实际会跳转到 `index.html?mode=neighbor`
  - 已默认隐藏：导出、快照、复杂测量与部分内部核对项（保留户型展示、开发商/资料查看、水电点位）

## 临时公域（最快）

1) 启动：
```bash
cd '/Users/beverley/Documents/ChatGPT/室内设计/白模项目'
./public_share.sh 8765
```
2) 在另一终端启动公网穿透：
```bash
cd '/Users/beverley/Documents/ChatGPT/室内设计/白模项目'
npx -y localtunnel --port 8765
```
得到公网链接后，分享：
- 全量版：`https://xxx.loca.lt/`
- 邻居版：`https://xxx.loca.lt/neighbor.html`

> `localtunnel` 是临时隧道，建议只用于快速看图；稳定共享建议配合 GitHub Pages / Cloudflare Pages / Vercel。

## 推荐的长期方案（建议）

把 `白模项目` 目录里的内容发布到静态托管：
- GitHub Pages（最推荐）
  1. 把 `白模项目` 内容作为仓库根目录上传
  2. 开启 Pages 指向 `main` 分支根目录
  3. 邻居版链接直接用 `yourdomain/neighbor.html`
- Cloudflare Pages / Vercel 也可以，规则同上。

## 注意

- 当前 `viewer.js` 已支持 `?mode=neighbor`。
- 如需把邻居版改为独立路由（例如 `neighbor/` 文件夹），可以再加一层重定向，不影响现有功能。
